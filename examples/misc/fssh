#!/usr/bin/env perl
use strict;
use warnings;
use Accessories::Forks;

my $forks = Accessories::Forks->new;
my @commands = <>;
$forks->spawn(
    8,
    \@commands,
    sub {
        my ( $cmd, $out ) = @_;
        chomp $cmd;
        chomp $out;
        print "+ $cmd\n$out\n";
    }
);
